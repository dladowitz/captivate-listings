%h1 Sign up for a plan today

.col-md-4
  = simple_form_for [@user, @subscription] do |f|
    = f.input :level, as: :select, selected: "Silver", disabled: [2, 3], collection: { "Silver": 1, "Gold": 2, "Platinum": 3 }
    .actions
      %script.stripe-button{ src: "https://checkout.stripe.com/checkout.js",
        data: {amount: 1000, description: "Website Hosting", key: ENV["STRIPE_PUBLISHABLE_KEY"]}}

    -# = f.button :submit
