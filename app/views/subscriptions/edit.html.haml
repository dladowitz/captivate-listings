.row
  .col-md-6
    %h1
      %strong Subscription

    %h2
      %strong Level:
      - if @subscription
        Silver
        %br
        = button_to "Cancel", user_subscription_path(@user, @subscription), :method => :delete, class: "btn btn-danger mt10", data: { confirm: "Are you sure?" }
